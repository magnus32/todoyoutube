{% extends 'base.html.twig' %}

{% block body %}
    <h2 class="page-header">Latest News</h2>
    <table class="table table-striped">
        <thead>
        <tr>
            <th>#</th> <th>Todo</th> <th>Due date</th><th></th>
        </tr>
        </thead>
        <tbody>

            {% for todo in todos %}
                <tr>
                <td scope="row">{{ todo.id }}</td>
                <td>{{ todo.name }}</td>
                <td>{{ todo.dueDate|date('F j, Y, g:i a') }}</td>
                <td>
                <a href="{{ path('todo_details', {'id': todo.id}) }}" class="btn btn-success">View</a>
                <a href="{{ path('todo_edit', {'id': todo.id}) }}" class="btn btn-default">Edit</a>
                <a href="{{ path('todo_delete', {'id': todo.id}) }}" class="btn btn-danger">Delete</a>
                </td>
                </tr>
            {% endfor %}

        {#<tr>
            <th scope="row">1</th>
            <td>NAME</td>
            <td>DUE DATE</td>
            <td>
                <a href="{{ path('todo_details', {'id': 'x'}) }}" class="btn btn-success">View</a>
                <a href="{{ path('todo_edit', {'id': 'x'}) }}" class="btn btn-default">Edit</a>
                <a href="{{ path('todo_delete', {'id': 'x'}) }}" class="btn btn-danger">Delete</a>
            </td>
        </tr>#}

        </tbody>
    </table>
{% endblock %}
